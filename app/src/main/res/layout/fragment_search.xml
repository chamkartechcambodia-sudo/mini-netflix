<?xml version="1.0" encoding="utf-8"?>
<!-- Sprint 8 — Search screen: a top input field + a 3-column grid of results
     (re-using the same MoviePosterAdapter as Home). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/search_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- Search bar at the top: dark surface, white text, white-ish hint. -->
    <EditText
        android:id="@+id/search_input"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_margin="12dp"
        android:background="#1A1A1A"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:hint="Search movies…"
        android:textColor="#FFFFFF"
        android:textColorHint="#808080"
        android:textSize="16sp"
        android:imeOptions="actionSearch"
        android:inputType="text"
        android:maxLines="1"
        android:autofillHints=""
        android:importantForAutofill="no" />

    <!-- Status text: shown above the grid when empty / loading / error. -->
    <TextView
        android:id="@+id/search_status_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:textColor="#B0B0B0"
        android:textSize="15sp"
        tools:text="Type to search movies." />

    <!-- Loading spinner shown WHILE the network is running. -->
    <ProgressBar
        android:id="@+id/search_loading"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="8dp"
        android:indeterminateTint="#E50914"
        android:visibility="gone" />

    <!-- 3-column grid of poster cards — re-uses MoviePosterAdapter + grid_view_item.xml. -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/search_results"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingStart="16dp"
        android:paddingTop="12dp"
        android:paddingEnd="8dp"
        android:paddingBottom="16dp"
        android:clipToPadding="false"
        android:layoutAnimation="@anim/layout_animation_fall_down"
        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
        app:spanCount="3"
        tools:listitem="@layout/grid_view_item" />
</LinearLayout>
